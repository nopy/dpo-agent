# dpo-agent web — environment variables
#
# Copy this file to .env and fill in the values. The .env file
# is loaded by docker compose automatically.
#
# DO NOT commit your .env file to git (it has secrets).

# ─── LLM provider credentials (one of these is required) ─────────────
# At least one of ANTHROPIC_API_KEY, OPENAI_API_KEY, or
# OPENROUTER_API_KEY must be set.
# If multiple are set, the priority is:
# ANTHROPIC_API_KEY > OPENROUTER_API_KEY > OPENAI_API_KEY
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# OpenRouter (https://openrouter.ai) — single API key for 100+ models.
# Get a key at https://openrouter.ai/keys
# Then set OPENROUTER_API_KEY=sk-or-v1-...
# Optionally pin the model via LLM_MODEL=anthropic/claude-sonnet-4
OPENROUTER_API_KEY=

# Optional: override the LLM provider selection.
# Values: "auto" (default, picks from env), "anthropic", "openai"
LLM_PROVIDER=auto

# ─── Model selection (one per kind) ──────────────────────────
# Each task can use a different model. Set DPO_AGENT_MODEL_{KIND}
# to override the default for that kind.
#
# Kinds:
#   - LOW    : cheap/fast model (navigator, kg_resolve, kg_update)
#   - MEDIUM : default model (reviewer, kg_extract, kg_agent)
#   - HIGH   : strong/expensive model (critique, kg_verify)
#
# Set just one of these to change the model for a specific role.
# Or set LLM_MODEL above to change all kinds at once.
#
# Examples:
#   DPO_AGENT_MODEL_LOW=claude-haiku-4-5      # navigator
#   DPO_AGENT_MODEL_MEDIUM=claude-sonnet-5     # reviewer
#   DPO_AGENT_MODEL_HIGH=claude-opus-4-5       # critique
#   DPO_AGENT_MODEL_MEDIUM=openai/gpt-4o      # use GPT-4o for review
#   DPO_AGENT_MODEL_HIGH=anthropic/claude-sonnet-4-5  # use Sonnet for critique
#
# For OpenRouter, use the provider/model-name format:
#   DPO_AGENT_MODEL_MEDIUM=anthropic/claude-sonnet-4
#   DPO_AGENT_MODEL_HIGH=openai/o1
#
# When using OpenRouter, OPENROUTER_API_KEY (above) is the auth.

LLM_MODEL=

# ─── Neo4j (knowledge graph) ───────────────────────────────────────────
# Used by the kg_build task to write to a real Neo4j instance.
# If not set, the kgpipeline falls back to SQLite (the default).
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=devpassword

# ─── Auth for Neo4j inside the compose network ────────────────────────
# Same value as NEO4J_PASSWORD but in Neo4j's expected format.
NEO4J_AUTH=neo4j/devpassword

# ─── Web server config ──────────────────────────────────────────────────
DPO_AGENT_LOG_LEVEL=info
# Comma-separated list of allowed CORS origins, or * for any.
# In production, replace * with your domain (e.g. https://dpo.example.com).
DPO_AGENT_CORS_ORIGINS=*
