"""Integrations with external agent frameworks (langchain,
deepagents, etc.).

Each integration is a separate module to keep the core
package lightweight. The langchain integration is the
reference; other frameworks (e.g. crewai, autogen) can
follow the same pattern.
"""
