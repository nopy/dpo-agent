"""Examples submodule — in-memory DocumentTools, CLI, FastAPI server.

This submodule ships with the package so users have runnable
examples. Production code should implement its own DocumentTools
bound to the real document store; the in-memory store is for
testing and local development.
"""
